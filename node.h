#include <iostream>

class Node{
    public:
    //Constructors
        Node();
        Node(char NodeType);
    //Mutators
    //Accessors
    //Destructors
        ~Node();
    private:
        char NodeType;
        int population;
        int pollution;
        int workers;
        int goods
};